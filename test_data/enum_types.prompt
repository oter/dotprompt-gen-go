---
model: openai/gpt-4
input:
  schema:
    type: object
    properties:
      priority:
        type: string
        enum: [low, medium, high]
        description: Task priority level
      status:
        type: string
        enum: [pending, approved, rejected]
        description: Approval status
    required: [priority, status]
output:
  schema:
    type: object
    properties:
      result:
        type: string
        enum: [success, failure, retry]
        description: Processing result
      level:
        type: integer
        enum: [1, 2, 3, 4, 5]
        description: Result level
    required: [result, level]
---
Process priority {{priority}} with status {{status}}.
